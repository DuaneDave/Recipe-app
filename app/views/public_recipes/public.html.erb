<section class="public-recipe">
  <h2>Public Recipes</h2><br>
  <article>
  <% @public_recipes.each do |recipe| %>
    <div class="public-list">
      <h3><%= link_to recipe.name, recipe_path(recipe) %></h3>
      <p>By: <%= recipe.user.name %></p>
    </div>
  <% end %>
  </article>
</section>
